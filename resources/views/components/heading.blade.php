@props(['title', 'subtitle'])

<div>
  <h3 class="text-base font-semibold leading-7 text-gray-900">{{ $title }}</h3>
  <p class="max-w-2xl mt-1 text-sm leading-6 text-gray-500">{{ $subtitle }}</p>
</div>
