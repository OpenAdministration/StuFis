#!/bin/bash
### Bring StuFiS back out of maintenance mode.
# Re-exec under bash if started via `sh stufis-up`.
[ -n "${BASH_VERSION:-}" ] || exec bash "$0" "$@"
set -e

source "$(dirname -- "${BASH_SOURCE[0]}")/stufis-common"

php artisan up "$@"
